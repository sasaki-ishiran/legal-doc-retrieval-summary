# ⚖️ 法律文书智能检索与信息抽取系统

本项目是一个面向本科毕业设计的法律文书智能检索与辅助分析系统。系统围绕法律文书检索场景，集成关键词检索、语义向量检索、混合检索、推荐理由展示、摘要生成、关键要素抽取与检索评测功能。

项目当前支持两种运行方式：

1. **演示模式**：当前电脑没有 LeCaRD 数据集、MySQL 数据库或 FAISS 索引时，系统会自动使用内置样例案例启动，方便调试界面和答辩演示。
2. **真实数据模式**：回到有数据集的电脑后，导入 LeCaRD 数据，构建 FAISS 索引，系统会自动切换到真实数据检索。

---

## 1. 项目定位

论文可以围绕以下题目展开：

> 基于语义向量与混合检索的法律文书智能检索系统设计与实现

系统目标不是训练一个全新的大模型，而是在已有法律文书数据集基础上，完成一个具有工程完整性和一定实验分析深度的检索应用原型。

主要解决的问题：

- 传统关键词检索难以理解案情语义。
- 单一向量检索缺少关键词可解释性。
- 法律文书内容较长，用户需要摘要和关键信息辅助理解。
- 毕业论文需要实验评价指标支撑系统有效性。

---

## 2. 功能模块

| 模块 | 文件 | 说明 |
|---|---|---|
| 统一配置 | `LeCaDR/config.py` | 管理数据库、模型、索引路径、检索参数和演示模式 |
| 数据导入 | `LeCaDR/data_import.py` | 递归读取 LeCaRD JSON 文书并写入 MySQL |
| 索引构建 | `LeCaDR/build_index.py` | 使用 SentenceTransformer 生成向量并构建 FAISS 索引 |
| 检索逻辑 | `LeCaDR/retrieval.py` | 实现关键词检索、语义结果包装和混合检索融合 |
| 文本处理 | `LeCaDR/text_utils.py` | 文本清洗、关键词命中、片段截取、推荐理由生成 |
| 智能分析 | `LeCaDR/llm_utils.py` | 调用 Ollama 进行摘要和要素抽取，失败时规则兜底 |
| 评测指标 | `LeCaDR/evaluation.py` | Precision@K、Recall@K、MRR、nDCG@K |
| 演示数据 | `LeCaDR/sample_data.py` | 无数据集环境下的内置案例 |
| Web 界面 | `LeCaDR/app.py` | Gradio 交互界面 |
| 评测模板 | `LeCaDR/evaluation_cases.json` | 示例查询和相关案例编号 |

---

## 3. 系统架构

```text
用户查询
  │
  ├── 关键词检索：轻量词项重合度评分
  │
  ├── 语义检索：Sentence-BERT / BGE 向量编码 + FAISS 相似度搜索
  │
  └── 混合检索：语义分数与关键词分数加权融合
        │
        ├── 推荐理由与命中片段展示
        ├── 法律文书正文与判决结果展示
        ├── LLM / 规则方法生成摘要与要素抽取
        └── Precision@K、Recall@K、MRR、nDCG@K 实验评价
```

混合检索默认使用如下融合思想：

```text
最终分数 = 语义分数 × 0.7 + 关键词分数 × 0.3
```

权重可在 `LeCaDR/config.py` 中调整。

---

## 4. 运行环境

推荐环境：

- Python 3.9+
- MySQL 5.7+
- Windows / Linux 均可
- 可选：Ollama 本地大模型服务

安装依赖：

```bash
pip install -r requirements.txt
```

---

## 5. 演示模式运行

当前电脑没有真实数据集时，直接运行：

```bash
python LeCaDR/app.py
```

系统会自动检查：

- 依赖库是否完整。
- MySQL 是否可连接。
- FAISS 索引文件是否存在。
- 数据库中是否有案例数据。

如果检查失败，会自动进入演示模式，并使用 `LeCaDR/sample_data.py` 中的案例启动界面。

访问地址：

```text
http://localhost:7860
```

演示模式可用于：

- 检索模式展示。
- 界面截图。
- 推荐理由展示。
- 摘要和要素抽取功能演示。
- 评测模块流程演示。

注意：演示数据只用于系统展示，不建议作为论文实验结果。

---

## 6. 真实数据模式运行

回到有 LeCaRD 数据集的电脑后，按以下步骤运行。

### 6.1 创建数据库

```sql
CREATE DATABASE IF NOT EXISTS legal_ir DEFAULT CHARSET utf8mb4;
USE legal_ir;
```

`LeCaDR/data_import.py` 会自动创建 `cases` 表。

### 6.2 修改配置

在 `LeCaDR/config.py` 中修改：

```python
data_root_path = r"你的 LeCaRD candidates 目录路径"
```

也可以通过环境变量覆盖：

```bash
set LEGAL_IR_DATA_ROOT=D:\path\to\LeCaRD-main\data\candidates
set LEGAL_IR_DB_PASSWORD=你的数据库密码
```

### 6.3 导入数据

```bash
python LeCaDR/data_import.py
```

### 6.4 构建向量索引

```bash
python LeCaDR/build_index.py
```

生成：

- `LeCaDR/legal_vector.index`
- `LeCaDR/db_ids.npy`

### 6.5 启动系统

```bash
python LeCaDR/app.py
```

如果数据库和索引加载成功，系统会显示“真实数据模式”。

---

## 7. 检索模式说明

### 7.1 关键词检索

关键词检索使用轻量级词项重合度，对查询和文书标题、正文、判决结果进行匹配。

优点：

- 实现简单。
- 可解释性强。
- 适合作为论文中的基线方法。

不足：

- 对同义表达、长文本语义理解能力较弱。

### 7.2 语义检索

语义检索使用 SentenceTransformer 模型将查询和文书转化为向量，然后使用 FAISS 进行相似度检索。

优点：

- 能捕捉语义相似性。
- 对自然语言案情描述更加友好。

不足：

- 对具体关键词和法律条款命中的解释性较弱。

### 7.3 混合检索

混合检索融合关键词分数和语义分数，兼顾语义理解和关键词可解释性。

论文中可以将其作为系统改进方法，与关键词检索、语义检索做对比实验。

---

## 8. 检索评测

项目提供以下评价指标：

| 指标 | 含义 |
|---|---|
| Precision@K | 前 K 个检索结果中相关文书比例 |
| Recall@K | 相关文书中被前 K 个结果召回的比例 |
| MRR | 第一个相关结果出现位置的倒数 |
| nDCG@K | 考虑排序位置的归一化折损累计增益 |

评测样例文件：

```text
LeCaDR/evaluation_cases.json
```

格式示例：

```json
[
  {
    "query": "组织传销 发展下线 层级返利",
    "relevant_ids": [6]
  }
]
```

真实论文实验建议准备 10 到 30 条查询，每条人工标注 1 到若干相关案例编号，然后分别测试：

1. 关键词检索。
2. 语义检索。
3. 混合检索。

论文实验表格可以写成：

| 方法 | Precision@5 | Recall@5 | MRR | nDCG@5 |
|---|---:|---:|---:|---:|
| 关键词检索 | 待实验 | 待实验 | 待实验 | 待实验 |
| 语义检索 | 待实验 | 待实验 | 待实验 | 待实验 |
| 混合检索 | 待实验 | 待实验 | 待实验 | 待实验 |

---

## 9. 论文写作对应关系

| 论文章节 | 可写内容 |
|---|---|
| 绪论 | 法律文书检索背景、传统检索不足、智能检索意义 |
| 相关技术 | 文本预处理、Sentence-BERT、FAISS、混合检索、LLM 信息抽取 |
| 需求分析 | 文书检索、案例查看、智能分析、评测实验、演示模式 |
| 系统设计 | 配置层、数据层、检索层、融合排序层、分析层、展示层 |
| 系统实现 | 结合各 Python 文件说明功能实现 |
| 实验分析 | 关键词检索、语义检索、混合检索对比 |
| 总结展望 | 数据规模、模型微调、法律推理能力、标注数据不足 |

---

## 10. 后续可扩展方向

如果时间允许，可以继续扩展：

- 引入 BM25 替代当前轻量关键词检索。
- 增加案由、法院、年份等筛选条件。
- 使用交叉编码器进行重排序。
- 将评测结果导出为 CSV。
- 增加数据统计页面，如案由分布、文书长度分布。
- 增加用户查询日志，用于后续系统分析。

---

## 11. 项目说明

本系统为毕业设计原型，重点展示法律文书智能检索系统的完整流程：数据导入、向量索引、检索融合、智能分析、实验评测和 Web 展示。

演示模式保证无数据环境下可运行；真实数据模式用于论文实验和最终答辩展示。
